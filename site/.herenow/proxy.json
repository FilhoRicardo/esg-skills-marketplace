{
  "proxies": {
    "/api/intake/submit": {
      "upstream": "https://REPLACE_WITH_TUNNEL_URL/intake",
      "method": "POST",
      "rateLimit": "6/hour/ip",
      "headers": {
        "Authorization": "Bearer ${MAC_MINI_INTAKE_SECRET}"
      }
    }
  }
}
