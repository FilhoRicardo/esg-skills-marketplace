# Approved skills

Only skill folders merged into `main` belong here. Each folder must contain:

- `SKILL.md` with valid `name` and `description` frontmatter;
- `marketplace.json` with a display title and approved category;
- optional text-only references.

Use [`templates/skill-template/`](../templates/skill-template/) for a new submission.
